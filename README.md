## Twitter Server

1. Create `.env` file

   ```sh
   cp .env.example .env
   ```

2. replace `XXX` value in the `.env` file with yours Twitter API credentials

3. Launch server

   ```sh
   yarn start
   ```

   or

   ```sh
   npm start
   ```
